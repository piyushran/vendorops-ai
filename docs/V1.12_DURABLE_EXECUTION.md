# V1.12 Durable Execution

The production execution foundation persists run/tool state before external side effects, uses database claims and worker leases, and reconciles expired executions using the original idempotency key. Unsafe ambiguous outcomes are not blindly replayed.
