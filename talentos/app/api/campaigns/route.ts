import {campaigns} from '@/lib/data';export async function GET(){return Response.json({campaigns})}export async function POST(req:Request){const body=await req.json().catch(()=>({}));return Response.json({id:`cmp_${Date.now()}`,status:'ready_for_approval',approvalRequired:true,objective:body.objective||'Increase qualified talent demand',createdAt:new Date().toISOString()},{status:201})}
