export async function POST(req:Request){const body=await req.json().catch(()=>({}));const objective=String(body.objective||'Diagnose talent demand');return Response.json({status:'completed',objective,runId:`run_${Date.now()}`,steps:[{agent:'Perception Agent',status:'complete',result:'Mapped employer perception signals'},{agent:'Competitive Agent',status:'complete',result:'Benchmarked 11 tracked employers'},{agent:'Decision Engine',status:'complete',result:'Generated evidence-backed recommendation'}],recommendation:'Launch an employee-led engineering ownership campaign with approval gates.'});}
